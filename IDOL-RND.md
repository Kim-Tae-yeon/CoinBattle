# 이번 생은 아이돌 — 0.4.0 / 반년의 선택

이 브랜치의 아이돌 게임은 `index.html`과 `season-r4/`에서 실행됩니다. 다른 CoinBattle 폴더와 main 브랜치는 변경하지 않습니다. 외부 패키지나 API 키 없이 정적 서버로 실행합니다.

## 기획 자가피드백 기준

- 숫자만 비슷하게 오르는 반복 선택을 합치고, 기본 시간 단위를 반년으로 유지한다.
- 주요 선택에는 관련 조직의 제안과 플레이어의 속마음이 먼저 나온다. 힌트는 정답이 아니라 판단축을 제시한다.
- 확정 효과, 예상 범위, 추가 위험, 지속 효과를 구분한다. 미리보기와 실제 실행은 같은 계산 함수를 사용한다.
- 결과는 이전 → 현재와 실제 변화량으로 남긴다. 지나가는 연출을 놓쳐도 직전 결과와 기록에서 다시 읽을 수 있어야 한다.
- 선택 화면에는 필요한 상태와 모든 선택지를 넣는다. 결과·정산·기록은 스크롤을 허용한다.
- 방향성은 실제 성장 보정과 후속 사건의 조건을 바꿔야 한다. 태그만 붙이고 아무 변화도 없는 선택은 만들지 않는다.
- 랜덤은 드러낸 범위와 위험 안에서 작동한다. 화면을 다시 그리는 것만으로 선택지나 결과가 바뀌어서는 안 된다.
- 중요한 선택이 회고에 남아야 한다. 계약 변경과 포지션 전환의 이득뿐 아니라 잃는 보정도 표시한다.

## 구현 범위

13세 상반기부터 50세까지 반년 단위로 진행합니다. 한 시즌의 반복 수업은 방향 선택 하나로 압축됩니다. 학원·회사 제안은 단계, 성장 방향, 획득 태그와 사건 재등장 간격에 따라 도착합니다. 현재 27종의 제안과 81개 대응 선택지가 있습니다.

입사·데뷔조·데뷔 확정은 각 3회의 평가 카드로 진행합니다. 집중과 컨디션을 다음 어필과 연결합니다. 장기 활동은 팬, 브랜드, 인성, 컨디션 및 스트레스로 판정합니다. 실제 업계 통계가 아닌 가상 계약으로 정산하며 회사 몫, 제작 비용, 분배 인원, 빚 상환과 입금을 분리합니다.

보컬 전문반은 이후 2시즌의 보컬 성장에 +40%를 적용합니다. 포지션 태그는 해당 성장에 +15%를 적용하며 메인보컬은 OST, 센터는 표지 촬영, 퍼포먼스 리더는 안무 제작 제안을 엽니다. 재계약은 아티스트 몫을 50%에서 60%로 바꾸고 솔로 전환은 정산 인원을 5명에서 1명으로 바꿉니다. 디너쇼 태그는 콘서트 매출 +25% 및 레전드 조건에 사용됩니다.

결과 숫자 보간, 짧은 선택 피드백과 주요 전환의 파티클을 구현했습니다. 시스템 모션 줄이기를 우선하며 진동은 설정에서 켠 지원 브라우저에서만 사용합니다. 기본 화면에 자동 진행을 강제하지 않습니다.

## 실행과 회귀 테스트

```sh
python3 -m http.server 8000
node season-r4/test.cjs
```

2026-09-16 검증 결과:

- Node: 100개 고정 시드로 입문부터 종료까지 실행. 114,968개 assertion 통과. 미리보기와 실행 변화량, 수치 유효성, 이중 선택 방지, 태그·버프 반영, 모든 이벤트 옵션을 검사했습니다.
- Chromium: 7개 세로 뷰포트(393×720, 360×640, 412×740, 393×680, 360×600, 320×568, 430×810), 각 234개 대사·선택 상태, 총 1,638회 렌더 검사에서 화면/카드 넘침과 JavaScript 예외가 없었습니다.
- Chromium 네이티브 DOM 클릭 핸들러: 296개 진행 단계, 112개 의사결정으로 50세 엔딩까지 확인. 이중 입력, 결과 유지, 정보 모달의 무변경, 가로 안내, 별도 레전드 엔딩 fixture를 확인했습니다.

브라우저 테스트는 정확한 로컬 파일을 묶어 `set_content`로 실행했습니다. 이 환경에서 URL 탐색이 차단되어 공개 URL의 브라우저 E2E 검증과 S25 실기기 검증은 하지 못했습니다. 저장/복원은 테스트용 storage facade로 확인했으며 실제 HTTPS origin의 영속 저장 검증과 구분합니다.

## 남은 R&D

8~10분은 설계 목표이지 사용자 플레이 측정값이 아닙니다. 보수적인 자동 선택 정책은 100회 모두 장수 아이돌로 끝났으므로 이 결과를 일반 사용자 성공률로 해석하면 안 됩니다. 장기 구간의 결정 수, 난이도, 엔딩 분포와 실제 읽기 시간은 사람의 플레이로 추가 검증해야 합니다. 이 빌드는 의미 없는 반복을 반년 계획으로 대체했지만 전체 터치 수가 절반이 되었다고 주장하지 않습니다.

새 저장 키는 `idol-longrun-season-040`입니다. 이전 미완성 빌드의 저장 구조를 새 규칙으로 자동 변환하지 않습니다. 다른 화면으로 전환해도 선택지를 다시 추첨하지 않습니다.
